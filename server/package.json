{
  "name": "server",
  "version": "1.0.0",
  "description": "server folder to serve as API / DB for gatsby.js",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jefferson Oliveira",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
