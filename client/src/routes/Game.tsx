import * as React from "react";
import styled from "styled-components";

const Container = styled.div``;

const Game: React.FC = () => {
  return (
    <Container>
      <h1>Game</h1>
    </Container>
  );
};

export default Game;
